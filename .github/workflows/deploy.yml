name: Deploy Next.js App

on:
<<<<<<< HEAD
  push:
    branches: ["main"]
=======
  # Automatic deployment on push to main has been disabled
  # Use workflow_dispatch for manual deployment only
  # push:
  #   branches: ["main"]
>>>>>>> origin/main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
<<<<<<< HEAD
          cache: 'npm'
          cache-dependency-path: package-lock.json
=======

      - name: Cache npm dependencies
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
>>>>>>> origin/main

      - name: Install dependencies
        run: npm ci

      - name: Build Next.js app
        run: npm run build

      - name: Upload production build
        uses: actions/upload-artifact@v4
        with:
          name: nextjs-build
<<<<<<< HEAD
          path: |
            .next
            package.json
            package-lock.json
            next.config.mjs
            public
=======
          path: .next
>>>>>>> origin/main
