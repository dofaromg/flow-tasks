name: PR Validation v1 legacy archive

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-redirect:
    name: Legacy workflow preserved
    runs-on: ubuntu-latest
    steps:
      - name: Show migration target
        run: |
          echo "Legacy source: docs/workflow-archive/pr-validation-v1.yml.txt"
          echo "Active parallel validator: .github/workflows/mrl-pr-validation-v2.yml"
