# 800-AI Organization Integration

The system maps the established enterprise organization to eight engineering role families. Allocation totals exactly 800 logical agents. A task is routed to a minimal role chain, then may fan out to multiple logical instances in future scheduler versions.

The physics-law project is integrated as `physics_auditor`, not maintained as an unrelated utility. Its outputs become evidence consumed by Reviewer and Optimizer roles.
