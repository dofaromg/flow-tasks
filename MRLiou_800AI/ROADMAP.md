# ROADMAP — Meta-code to 800-AI Integrated Runtime

## Lineage

1. **Meta-code / variable environment** — modular laws, reversible state, observable interfaces.
2. **AI SuperComputer / FlowCore** — trace anchor, snapshots, module manifests, local HTTP runtime.
3. **MRL L0–L7 particle system** — origin, compute, structure, memory, world, field, cognition, execution.
4. **MRLiou 800 AI enterprise organization** — enterprise roles mapped to technical runtime roles.
5. **Engineering multi-agent workflow** — architect → engineer → reviewer → optimizer, augmented by debugging, refactoring, UI, and physics audit.
6. **Physics-law audit branch** — CFD mass/momentum/energy terms, residual diagnostics, reports and future PIML interface.

## v1.0.0 — Delivered in this package

- Unified agent registry and organization allocator.
- Engineering wake gate and audit supervisor.
- Local HTTP API and CLI.
- Reversible datastore and chained trace evidence.
- 2D/3D mass-conservation checker.
- Momentum/energy extension contracts and documentation.
- GitHub Actions, Docker, systemd, Windows/Linux scripts.
- Full package manifest, SHA-256 list and dependency map.

## v1.1

- Momentum and energy residual builders as first-class runtime endpoints.
- NetCDF and CSV-folder loaders.
- HTML reports with term-by-term heatmaps and time-series plots.
- Persistent SQLite run index and trend analysis.

## v1.2

- GitHub repository ingestion and codebase dependency graph.
- Automatic PR review workflow using the Reviewer and Optimizer agents.
- OpenFOAM/SU2 adapters.
- PIML/PINN residual-loss adapter.

## v2.0

- DL580 multi-process scheduler.
- Four-VM parallel wave execution.
- Local model adapters and tool-execution sandbox.
- Signed module registry and release provenance.


## v1.1.0 — Windows Server deployment line

- Windows Server is now the canonical deployment target for this package.
- Added native PowerShell installation and verification.
- Added generated API-token authentication and secret-file Git exclusion.
- Added SYSTEM startup through Windows Task Scheduler.
- Added Domain/Private firewall helpers with LocalSubnet restriction.
- Added Windows operations, backup, status, stop, uninstall, and GitHub deployment scripts.
- Preserved Linux, Docker, and systemd assets as secondary compatibility channels.
