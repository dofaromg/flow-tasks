apiVersion: v1
kind: ConfigMap
metadata:
  name: orchestrator-config
  namespace: flowagent
  labels:
    app: orchestrator
data:
  MODULE_A_ENDPOINT: "http://module-a:8080"
  LOG_LEVEL: "INFO"
  ENVIRONMENT: "production"
  PORT: "8081"
