apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: orchestrator-pdb
  namespace: flowagent
  labels:
    app: orchestrator
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: orchestrator
